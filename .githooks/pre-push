#!/bin/sh
# Install once: git config core.hooksPath .githooks
# Blocks push if secrets or .env files would be published.

set -e
cd "$(git rev-parse --show-toplevel)"
node scripts/verify-no-secrets.mjs
